return {
  "shortcuts/no-neck-pain.nvim",
  version = "*",
  opts = {
    width = 130, -- or "textwidth" / "colorcolumn"
    mappings = {
      enabled = true,
      toggle = "<leader>np", -- default
    },
  },
}
